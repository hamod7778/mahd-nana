# 🚀 دليل رفع ونشر متجر "مهد ونعناع" أونلاين (Deployment Guide)

تهانينا على إكمال المتجر! يتكون مشروعك من إطار العمل **Next.js 14** وقاعدة بيانات **Prisma**. يمكنك رفع المتجر ليكون أونلاين ومتاح للعملاء بطريقتين مميزتين:

---

## 🌟 الخيار الأول (الأسهل والأسرع مجاناً): النشر على Vercel + قاعدة بيانات سحابية

تعتبر منصة **Vercel** هي الاستضافة الرسمية والأقوى لـ Next.js (سريعة جداً وتدعم ربط دومين خاص مجاناً).

### الخطوة 1: تجهيز قاعدة البيانات السحابية (Free PostgreSQL Database)
نظراً لأن منصات الاستضافة السحابية تحتاج قاعدة بيانات أونلاين:
1. أنشئ حساباً مجانياً على [Supabase.com](https://supabase.com) أو [Neon.tech](https://neon.tech).
2. أنشئ مشروعاً جديداً (New Project) واحصل على رابط قاعدة البيانات `DATABASE_URL`.
3. في ملف `prisma/schema.prisma` قم بتغيير السطر التالي:
   ```prisma
   datasource db {
     provider = "postgresql"
     url      = env("DATABASE_URL")
   }
   ```
4. نفّذ الأمر التالي لرفع الجداول لقاعدة البيانات الجديدة:
   ```bash
   npx prisma db push
   ```

### الخطوة 2: رفع الكود على GitHub
1. افتح موقع [GitHub.com](https://github.com) وأنشئ مستودعاً جديداً (New Repository) باسم `yemeni-baby-store`.
2. ارفع ملفات المشروع إليه.

### الخطوة 3: الربط مع Vercel
1. سجّل الدخول في [Vercel.com](https://vercel.com) بحساب GitHub.
2. اضغط **"Add New Project"** واختر مستودع `yemeni-baby-store`.
3. في خانة **Environment Variables** أضف المفتاح التالي:
   - `DATABASE_URL` = (رابط قاعدة البيانات الخاص بك من Supabase)
4. اضغط **Deploy**.
5. خلال 60 ثانية ستحصل على رابط أونلاين مجاني لمتجرك (مثال: `yemeni-baby-store.vercel.app`) ويمكنك ربط أي دومين خاص بك `.com` بضغطة زر!

---

## 🖥️ الخيار الثاني: النشر على سيرفر كامل (Hostinger VPS / DigitalOcean)

إذا كان لديك سيرفر خاص (VPS) وتريد إبقاء قاعدة البيانات الحالية SQLite كما هي بدون أي تغيير:

### الخطوات على السيرفر:
1. **رفع ملفات المشروع للسيرفر:**
   ```bash
   git clone <رابط_مستودعك>
   cd yemeni-baby-store
   ```

2. **تثبيت حزم الكود وبناء المشروع:**
   ```bash
   npm install
   npx prisma db push
   npm run build
   ```

3. **تشغيل المشروع 24/7 باستخدام PM2:**
   ```bash
   npm install -g pm2
   pm2 start npm --name "baby-store" -- start
   pm2 save
   ```

4. **ربط الدومين وتفعيل شهادة الأمان SSL (HTTPS):**
   - استخدام Nginx كـ Reverse Proxy لتوجيه البورت 3000 للدومين الخاص بك.
   - تفعيل شهادة الأمان المجانية باستخدام `certbot --nginx`.

---

## 🔒 القائمة المرجعية قبل إطلاق المتجر للعملاء (Pre-launch Checklist):

- [ ] التأكد من تحديث رقم الواتساب الخاص بالمتجر من لوحة التحكم (`/admin`).
- [ ] مراجعة وحفظ رسوم التوصيل للمحافظات اليمنية.
- [ ] التأكد من إضافة حسابات الصرافة اليمنية الفعلية (النجم، العمقي، الكريمي، الخ...).
- [ ] مراجعة وتحديث أسعار المنتجات وصور البنرات بالسلايدر.
