# 📖 دليل هيكلة وتعديل كود مشروع "متجر مهد ونعناع" 🌸

أهلاً بك! هذا الملف يشرح لك بالتفصيل شجرة ملفات المشروع كاملة، وظيفة كل ملف، وأين تذهب بالضبط إذا أردت تعديل أي جزء في المتجر أو لوحة التحكم.

---

## 📁 الهيكل العام للمشروع (Project Structure)

```
متجر مواليد/
├── prisma/                    # قاعدة البيانات ومخطط الجداول
│   ├── schema.prisma          # المخطط الرئيسي للجداول (Prisma Schema)
│   └── dev.db                 # ملف قاعدة البيانات المحتوي على البيانات (SQLite)
├── public/                    # الصور والملفات الثابتة
│   ├── logo.jpg               # الشعار الرسمي للمتجر
│   └── uploads/               # مجلد الصور المرفوعة من لوحة التحكم
├── src/                       # الكود المصدري للمشروع
│   ├── app/                   # الصفحات ومسارات الـ API (Next.js App Router)
│   │   ├── admin/             # لوحة التحكم ومتابعة الطلبات
│   │   ├── checkout/          # صفحة إتمام الطلب والحسابات والواتساب
│   │   ├── products/[id]/     # صفحة عرض تفاصيل المنتج والصور المتعددة
│   │   ├── track/             # صفحة تتبع حالة الطلب بالرقم أو الهاتف
│   │   ├── api/               # المسارات الخلفية والتعامل مع قاعدة البيانات (Backend APIs)
│   │   ├── layout.tsx         # الهيكل العام لكافة صفحات المتجر (Header & Footer)
│   │   ├── page.tsx           # الصفحة الرئيسية للمتجر
│   │   └── globals.css        # التنسيقات العامة والـ CSS
│   ├── components/            # العناصر المكونة للواجهة (Re-usable Components)
│   │   ├── Header.tsx         # الهيدر وشريط الملاحة والبحث
│   │   ├── Footer.tsx         # ترويسة أسفل الصفحة
│   │   ├── HeroSlider.tsx     # السلايدر الترويجي الفاخر
│   │   ├── ProductCard.tsx    # كارت عرض المنتج في الرئيسية
│   │   ├── ProductQuickViewModal.tsx # نافذة العرض السريع للمنتج
│   │   └── CartDrawer.tsx     # سلة المشتريات الجانبية (Slide-over Cart)
│   ├── context/               # إدارة الحالة العامة للمتجر
│   │   └── CartContext.tsx    # حفظ سلة المشتريات وحساب المبالغ
│   └── lib/                   # أدوات مساعدة وتشفير
│       ├── prisma.ts          # مجهّز الاتصال بقاعدة البيانات
│       └── auth.ts            # تشفير كلمة المرور وتأمين لوحة التحكم
├── package.json               # الحزم والمكتبات المعتمدة
└── tailwind.config.ts         # إعدادات التنسيقات والألوان
```

---

## 🗺️ أين تذهب عند الرغبة في التعديل؟ (Modification Guide)

### 1️⃣ التعديل على صفحات المتجر والواجهات (Frontend Pages)

| إذا كنت تريد تعديل: | اتجه إلى الملف التالي: |
| :--- | :--- |
| **الصفحة الرئيسية للمتجر** (البانر العلوي، توزيع الأقسام، طريقة عرض المنتجات) | 📄 `src/app/page.tsx` |
| **صفحة تفاصيل المنتج** (عرض الصور المتعددة، زر السلة، الوصف) | 📄 `src/app/products/[id]/page.tsx` |
| **صفحة إتمام الطلب والحسابات** (بيانات العميل، رسوم التوصيل، حساب الإجمالي) | 📄 `src/app/checkout/page.tsx` |
| **صفحة تتبع الطلبات** (الخط الزمني، تتبع الرقم/الهاتف) | 📄 `src/app/track/page.tsx` |
| **لوحة التحكم الكاملة** (إدارة المنتجات، البنرات، الطلبات، الواتساب، المحافظات) | 📄 `src/app/admin/page.tsx` |

---

### 2️⃣ التعديل على المكونات الهيكلية (Components)

| العنصر الذي تريد تعديله: | اتجه إلى الملف التالي: |
| :--- | :--- |
| **الشريط العلوي والهيدر** (الشعار، البحث، زر السلة) | 📄 `src/components/Header.tsx` |
| **السلايدر الرئيسي البانر** (طريقة الانتقال، الحجم، الحواف) | 📄 `src/components/HeroSlider.tsx` |
| **كارت المنتج في الرئيسية** (السعر، الأزرار، التوفر) | 📄 `src/components/ProductCard.tsx` |
| **نافذة العرض السريع للمنتج** (الـ Quick View Modal) | 📄 `src/components/ProductQuickViewModal.tsx` |
| **سلة المشتريات الجانبية** (الدرور، الأزرار، المجموع) | 📄 `src/components/CartDrawer.tsx` |
| **أسفل الصفحة (الفوتر)** (الروابط، الحقوق، شبكات الصرافة) | 📄 `src/components/Footer.tsx` |

---

### 3️⃣ التعديل على البرمجيات الخلفية والـ API (Backend Routes)

كل مسار في المجلد `src/app/api/` مسؤول عن وظيفة خلفية محددة:

- 🛍️ **المنتجات:** `src/app/api/products/route.ts` & `[id]/route.ts`
- 🍼 **الأقسام:** `src/app/api/categories/route.ts` & `[id]/route.ts`
- 🖼️ **البنرات والسلايدر:** `src/app/api/banners/route.ts` & `[id]/route.ts`
- 🚚 **رسوم التوصيل للمحافظات:** `src/app/api/governorates/route.ts` & `[id]/route.ts`
- 💳 **حسابات الصرافة:** `src/app/api/exchange-accounts/route.ts` & `[id]/route.ts`
- 📋 **الطلبات ورسائل الواتساب:** `src/app/api/orders/route.ts` & `[id]/route.ts`
- 📁 **رفع الصور من الجهاز:** `src/app/api/upload/route.ts`
- 🔐 **الأمان وتأمين الداشبورد:** `src/app/api/admin/login/route.ts`

---

### 4️⃣ التعديل على قاعدة البيانات والأنماط (Database & Auth)

- **إضافة حقول جديدة بالمنتجات أو الطلبات:** تعديل ملف `prisma/schema.prisma` ثم تنفيذ الأمر `npx prisma db push`.
- **تغيير كلمة مرور أدمن لوحة التحكم:** يتم تغيير التشفير في `src/lib/auth.ts` (كلمة المرور الحالية المشفّرة هي `admin425477`).

---

⚡ **ملاحظة تشغيل مشروعك:**
لتشغيل المتجر محلياً في أي وقت، افتح موجه الأوامر في مجلد المشروع ونفّذ:
```bash
npm run dev
```
ثم افتح الرابط: `http://localhost:3000`
ولوحة التحكم: `http://localhost:3000/admin`
